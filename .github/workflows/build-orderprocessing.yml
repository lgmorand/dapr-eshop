name: Build eshop.orderprocessing image

on:
  push:
    paths:
      - '**/eshop.orderprocessing/**'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    
    - uses: docker/build-push-action@v1
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
        repository: lgmorand/eshop-orderprocessing
        tags: latest
        dockerfile: src/eshop.orderprocessing/Dockerfile
    